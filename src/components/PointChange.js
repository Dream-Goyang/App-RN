export default function PointChange(walk) {
    let point = 100
    let newpoint = 0
    let newwalk = 0
    return (
        newpoint = point + (walk-(walk%100))/100
    )
    return (
        newwalk = walk%100
    )
}